[package]
name = "io"
version = "0.1.0"
edition = "2021"

[dependencies]
ir = { path = "../ir" }
sim = { path = "../sim" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
